stages:
   - work

test:
  work:
    stage: test
    image: golang:1.13
    script: 
     - go mod init feniks
     - go test .

build:
  work:
    stage: build
    image: docker:latest
    script:
     - docker build .

